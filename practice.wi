define a kind of object called Rohit:
    define init(self):
        self.age = 18
    define showage(self):
        print self.age
    define setage(self,age):
        self.age=age
        print "change to "
        print str(self.age)
smallr is a kind of Rohit
print smallr.age
smallr.setage(12)